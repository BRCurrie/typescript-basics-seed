// Run `tsc` then `node dist/app.js` to see result in console.

let sizes: string[];

sizes = ['small', 'medium', 'large'];

// Generic types. Below it expects an array full of strings.
let toppings: Array<string>;

toppings = ['pepperoni', 'bacon', 'olives', 'pineapple'];

// Contstructor style
// new Array()
